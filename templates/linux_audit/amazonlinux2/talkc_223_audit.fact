#!/bin/bash
# ** AUTO GENERATED **

# 2.2.3 - Ensure talk Client is not installed (Scored)

if [ $(sudo rpm -qa talk | wc -l) -ne 0 ]; then
    echo "{ \"talkc_223_audit\" : \"FAILED\"}"
else
    echo "{ \"talkc_223_audit\" : \"PASSED\"}"
fi

